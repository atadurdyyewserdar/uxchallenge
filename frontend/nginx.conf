server {
    listen 80;
    server_name uxstudio.serdarr.dev;

    # ACME challenge location for certbot (must be accessible over HTTP)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }

    # Proxy API requests (kept here only for safety—HTTPS server will handle proxy in production)
    location /api/ {
        proxy_pass http://backend:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# SSL server (temporarily disabled — certificates not present yet)
# NOTE: This block is intentionally removed so nginx can start and serve the ACME
# challenge over HTTP at /.well-known/acme-challenge/. After obtaining certificates
# with certbot, restore or add an SSL server block that points to
# /etc/letsencrypt/live/uxstudio.serdarr.dev/fullchain.pem and privkey.
#
# Example to re-enable:
# server {
#   listen 443 ssl http2;
#   server_name uxstudio.serdarr.dev;
#   ssl_certificate /etc/letsencrypt/live/uxstudio.serdarr.dev/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/uxstudio.serdarr.dev/privkey.pem;
#   ... (same content as before) ...
# }
