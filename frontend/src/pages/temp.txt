<Modal
            isOpen={modalIsOpen}
            onClose={() => openModal(false)}
            title="Edit contact"
          >
            {/* The Image Section */}
            <div className="flex items-center gap-4 mb-6">
              <div className="h-16 w-16 overflow-hidden rounded-full border border-residential-30">
                <img
                  src="/avatar.jpg"
                  alt="User"
                  className="h-full w-full object-cover"
                />
              </div>
              <Button variant="secondary" className="px-4 py-2 text-sm">
                Change picture
              </Button>
              <Button variant="secondary" className="px-3 py-2">
                {/* Trash Icon */}
                <svg
                  width="20"
                  height="20"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    strokeLinecap="round"
                    strokeLinejoin="round"
                    strokeWidth="2"
                    d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"
                  ></path>
                </svg>
              </Button>
            </div>

            <div className="">
              <div className="flex">
                {/* 1. Primary (Solid Level 60) */}
                <Button icon={PlusIcon}>Add new</Button>

                {/* 2. Secondary (Transparent) */}
                <Button variant="secondary" icon={PlusIcon}>
                  Add new
                </Button>

                {/* 3. Special (Gradient Border + Fill) */}
                <Button variant="special" icon={PlusIcon}>
                  Add new
                </Button>
              </div>

              <h1>Primary button</h1>
              <div className="flex">
                <PrimaryButton icon={PlusIcon}>Add new</PrimaryButton>
                <PrimaryButton icon={PlusIcon}>Add second</PrimaryButton>
              </div>
              <div className="flex">
                <div className="bg-[#141414] p-10 flex gap-4">
                  {/* Secondary Button */}
                  <SecondaryButton icon={PlusIcon}>Add new</SecondaryButton>
                </div>
                <div className="bg-[#141414] p-10 flex gap-4">
                  {/* Secondary Button */}
                  <SecondaryButton icon={PlusIcon}>Add new</SecondaryButton>
                </div>
              </div>
              <TextField label="Name" placeholder="Example" />

              {/* Typed State (Finished) */}
              {/* Visually identical to Default, but with text value */}
              <TextField label="Phone number" defaultValue="+36 01 234 5678" />
            </div>
          </Modal>